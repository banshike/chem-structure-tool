# ============================================================
# ChemStructure Tool — Python 依赖
# ============================================================

# Web 框架
flask>=3.0

# 化学信息学核心库
rdkit>=2024.03

# HTTP 请求（调用 OPSIN / PubChem API）
requests>=2.31

# 图像处理（上传预处理）
Pillow>=10.0

# ── 图像识别 (OCSR, beta) ──────────────────────────────────
# 以下为可选的图像识别依赖，需要额外配置后才能启用：
#
# DECIMER (EfficientNet-V2 + Transformer):
#   pip install decimer
#   首次运行自动下载 285MB 模型，需要稳定的网络连接
#
# Img2Mol (CNN + CDDD Decoder):
#   已内置于 img2mol_repo/ 目录
#   需要额外配置 CDDD 解码器（Python 3.7 + TensorFlow 1.x）

